# loglensx Deliverables

This document summarizes the shipped project assets, runtime capabilities, and release readiness checks for `loglensx`.

## Package Information

| Field | Value |
| --- | --- |
| Package | `loglensx` |
| Current package metadata | `1.0.3` |
| Python support | Python 3.8+ |
| License | MIT |
| Package type | Python library with optional web integrations |
| Primary frameworks | Flask, FastAPI |

Before publishing a new release, verify that `pyproject.toml`, `setup.py`, and `loglensx/__init__.py` all use the same version.

## Delivered Components

### Core Library

| Path | Purpose |
| --- | --- |
| `loglensx/core/parser.py` | Parses `.log` files with built-in and custom regex patterns |
| `loglensx/core/analyzer.py` | Produces summaries, level counts, top loggers, error timelines, and filtered log slices |
| `loglensx/core/exporter.py` | Serializes parsed logs to JSON, CSV, and NDJSON |
| `loglensx/cli.py` | Provides summary, log filtering, file metadata, error-pattern, and export workflows |

### Web Integrations

| Path | Purpose |
| --- | --- |
| `loglensx/integrations/fastapi_integration.py` | Mounts dashboard, explorer, and JSON routes on a FastAPI app |
| `loglensx/integrations/flask_integration.py` | Mounts dashboard, explorer, and JSON routes on a Flask app |
| `loglensx/integrations/_dashboard.py` | Shared HTML, CSS, and JavaScript renderer for both integrations |
| `loglensx/integrations/__init__.py` | Exports integration setup helpers |

### Visualization Tools

| Path | Purpose |
| --- | --- |
| `loglensx/visualizers/charts.py` | Generates Plotly figure JSON for level, timeline, and logger charts |
| `loglensx/visualizers/tables.py` | Generates styled, searchable, sortable HTML tables |
| `visualizations/index.html` | Local viewer for generated Plotly JSON files |
| `visualizations/*.json` | Example chart payloads generated by the standalone workflow |

### Examples

| Path | Purpose |
| --- | --- |
| `examples/flask_example.py` | Complete Flask app using `setup_flask_loglensx` |
| `examples/fastapi_example.py` | Complete FastAPI app using `setup_fastapi_loglensx` |
| `examples/standalone_example.py` | Parser, analyzer, and chart JSON generation workflow |

### Tests

| Path | Coverage |
| --- | --- |
| `tests/test_loglensx.py` | Parser, analyzer, table rendering, and dashboard renderer behavior |

## Runtime Features

### Log Parsing

- Reads `.log` files from a configurable directory.
- Supports the default bracketed Python logging format.
- Supports simple and fallback parsing for common log styles.
- Accepts custom regex patterns with named groups and applies them before built-in patterns.
- Parses common JSON-line logs.
- Folds multiline tracebacks and stack frames into the preceding parsed log entry.
- Preserves source file and line number metadata.

### Analysis

- Total log summary.
- Counts by log level.
- Recent errors and warnings.
- Top logger ranking.
- Error frequency by hour.
- Recurring critical/error pattern grouping.
- Per-file statistics with parsed entry counts and first/last timestamps.
- Filters by level, logger, source file, search term, time window, and limit.

### Export

- JSON export for structured API and automation workflows.
- CSV export with flattened structured-log extras.
- NDJSON export for streaming-friendly downstream ingestion.

### Dashboard

- Shared professional UI for Flask and FastAPI.
- Metric cards for totals, errors, warnings, files, loggers, and stability.
- Interactive Plotly charts.
- Log explorer with server-side search, severity, logger, file, time-window, and limit filters.
- Client-side table search, level filtering, sorting, and expandable messages.
- Browser export action for filtered CSV downloads.
- Responsive layout for desktop and mobile use.

### APIs

When mounted at `/loglensx`, both Flask and FastAPI expose:

| Route | Purpose |
| --- | --- |
| `GET /loglensx/` | Dashboard UI |
| `GET /loglensx/logs` | Log explorer UI |
| `GET /loglensx/api/logs` | Filtered logs as JSON |
| `GET /loglensx/api/stats` | Summary, levels, top loggers, error frequency |
| `GET /loglensx/api/search` | Search results |
| `GET /loglensx/api/export` | Filtered exports as JSON, CSV, or NDJSON |
| `GET /loglensx/api/files` | Log file metadata |

## Dependencies

### Core

- `plotly>=5.0.0`
- `python-dateutil>=2.8.0`

### Optional Extras

- `loglensx[flask]`: Flask integration dependencies.
- `loglensx[fastapi]`: FastAPI and Uvicorn dependencies.
- `loglensx[dev]`: Test, lint, type-check, and publishing tools.

## Documentation Set

| File | Purpose |
| --- | --- |
| `README.md` | Main package documentation |
| `QUICKSTART.md` | Short setup guide for Flask, FastAPI, standalone usage, and visualization JSON |
| `PROJECT_SUMMARY.md` | Architecture and package overview |
| `PUBLISHING.md` | Release procedure |
| `PUBLICATION_CHECKLIST.md` | Release checklist |
| `CHANGELOG.md` | Version history |

## Verification Commands

Run syntax checks:

```bash
python -m compileall loglensx examples tests
```

Run tests:

```bash
pytest
```

Run coverage when `pytest-cov` is installed:

```bash
pytest --cov=loglensx --cov-report=html --cov-report=term-missing
```

Smoke-test the Flask example:

```bash
python examples/flask_example.py
```

Smoke-test the FastAPI example:

```bash
python examples/fastapi_example.py
```

View generated visualization JSON:

```bash
python -m http.server 8080
```

Open:

```text
http://127.0.0.1:8080/visualizations/
```

## Release Readiness Notes

- Confirm version consistency across package metadata before building.
- Clean old build artifacts before each release.
- Run `twine check dist/*` before upload.
- Test framework extras in a clean environment.
- Review `CHANGELOG.md` before tagging.
